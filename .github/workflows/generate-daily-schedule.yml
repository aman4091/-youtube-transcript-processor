name: Generate Daily Schedule

on:
  schedule:
    # Run at 2:00 AM UTC daily
    - cron: '0 2 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  generate-schedule:
    runs-on: ubuntu-latest

    steps:
      - name: Generate schedule for all users
        run: |
          curl -X POST \
            "${{ secrets.SUPABASE_URL }}/functions/v1/generate-schedule-for-all-users" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json"

      - name: Log completion
        run: echo "Daily schedule generated successfully at $(date)"
